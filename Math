// BEGIN (write your solution here)
const solution = (d, a, b) => {
  
  const rad = n => Math.PI / 180 * n; // функция для перевода градусов в радианы
  
  const c = Math.sin(rad(180 - a - b)); // находим значение противолежащего угла. Внимание - порядок вычислений важен!
                                        // сначала 180 - 60 - 60 = 60. Затем передал функции и перевел в радианы. Вычислили sin
  a = Math.sin(rad(a));                 // перевод в радианы и получение значения sin
  //console.log(a);
 
  b = Math.sin(rad(b));                 // перевод в радианы и получение значения sin
  //console.log(b);
 
  //console.log(c);
  
  d = Math.pow(d, 2) / 2;               // получаем готовое значение стороны  
  
  return d * ((a * b) / c);             // работаем с готовыми полученными значениями
  
};

export default solution;                // экспорт по умолчанию

// END

/*
solution.js
Реализуйте функцию которая считает площадь треугольника на основе известной стороны и прилегающих к ней углов. Углы задаются в градусах.
Экспортируйте ее по умолчанию.

Формула: eсли известна одна сторона треугольника и два прилежащих к ней угла, то площадь данного треугольника вычисляется, 
как половина квадрата данной стороны умноженная на дробь, в числителе которой, произведение синусов прилежащих углов, 
а в знаменателе синус противолежащего угла.

Противолежащий угол вычисляется по формуле: 180 - сумма двух известных углов.

solution(3, 60, 60); // 3.89711431703
Подсказки:
Так как тригонометрические функции в Math работают с радианами, вам будет необходимо преобразовывать 
градусы в радианы. Напишите для этого вспомогательную функцию. Формула для расчета: градусы * пи / 180.
*/
